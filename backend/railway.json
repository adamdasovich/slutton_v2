{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install --upgrade pip && pip install -r requirements.txt && python manage.py collectstatic --noinput --settings=slutton_backend.settings_production"
  },
  "deploy": {
    "startCommand": "gunicorn slutton_backend.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
